// ----------------------------------------------------------------------------------
// -- Hex region struct.
// ----------------------------------------------------------------------------------

var HexRegion = function()
{
    this.name = 'Default region name';
    this.tiles = [];
};

export default HexRegion;